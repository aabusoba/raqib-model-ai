events {
    worker_connections 1024;
}

http {
    include       /etc/nginx/mime.types;
    default_type  application/octet-stream;

    # Docker internal DNS resolver
    resolver 127.0.0.11 valid=10s ipv6=off;

    server {
        listen 9090;
        server_name raqib-ai.nafusah.tech localhost;

        # Standard Proxy Headers
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Forwarded-Host $host;

        # Central Health Check (Root)
        location / {
            default_type application/json;
            return 200 '{"status": "online", "system": "Raqib AI - Traffic DSS Hub", "version": "1.0.0", "message": "All systems operational. Access dashboards at /mod1/ and /mod2/"}';
        }

        # Model 1 Dashboard
        location /mod1/ {
            proxy_pass http://model1-dashboard:8501;
            
            # WebSocket Support
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";
            
            # Timeouts
            proxy_read_timeout 86400;
            proxy_send_timeout 86400;
            proxy_connect_timeout 60s;
        }

        # Model 1 API
        location /mod1/api/ {
            proxy_pass http://model1-api:5000/;
        }

        # Model 2 Dashboard
        location /mod2/ {
            proxy_pass http://model2-dashboard:8502;
            
            # WebSocket Support
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";
            
            # Timeouts
            proxy_read_timeout 86400;
            proxy_send_timeout 86400;
            proxy_connect_timeout 60s;
        }

        # Model 2 API
        location /mod2/api/ {
            proxy_pass http://model2-api:5001/;
        }
    }
}
